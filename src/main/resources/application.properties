spring.application.name=FLIGHTRADARPROXY

server.port=8080

#Metrics related configurations
management.endpoint.metrics.enabled=true
management.endpoints.web.exposure.include=*
management.endpoint.prometheus.enabled=true
management.metrics.export.prometheus.enabled=true
management.endpoint.health.show-details=always

info.app.name=FLIGHTRADARPROXY
info.app.description=Proxy HLA para o FlightRadar24
info.app.version=1.0.0

federation.fomfolder=/foms
federation.name=${FEDERATION_NAME}
federation.federateName=${FEDERATE_NAME}

#frcollector.firstrun=${FIRST_COLLECT}
#frcollector.interval=${COLLECT_INTERVAL}

frcollector.firstrun=1
frcollector.interval=5

logging.level.root=info


